modelo = []
consumo = []
for i in range(5):
	modelo.append(str.lower(input("Digite um modelo de carro: "))
	consumo.append(str.lower(input("Digite o consumo por litro: "))
	print(modelo)	
	print(consumo)
	for i in consumo:
		if consumo[i] > i:
		
			maisecono =  
	
